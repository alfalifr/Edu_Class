<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sidev.kuliah.tekber.edu_class">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_graduate_logo"
        android:label="@string/app_name_"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="sidev.kuliah.tekber.edu_class.app.App"
        android:theme="@style/AppTheme">
        <activity android:name="sidev.kuliah.tekber.edu_class.act.SimpleSingleFragActImpl" />
        <activity android:name="sidev.kuliah.tekber.edu_class.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>